@model VW.Forum.Web.ViewModels.Post.PostsListViewModel

<a asp-controller="Post" asp-action="CreatePost" class="btn3 btn3-blog3 ">
	<span>ADD POST</span>
</a>
<div class="container2">
	<div id="blog" class="row">

		@foreach (var post in Model.Posts)
		{
			<div class="col-md-10 blogShort">
				<h1>
					@post.Title
					<small class="pull-right">@post.DateCreated</small>
				</h1>
				<a asp-controller="Post" asp-action="Post" asp-route-id="@post.Id" class="btn btn-blog pull-right marginBottom10">
					<span>READ MORE</span>
				</a>
			</div>
		}

	</div>
</div>
